{
  "name": "isc2-cc",
  "version": "1.0.0",
  "private": true,
  "description": "ISC2 CC study app packaged with Capacitor.",
  "scripts": {
    "prepare:android": "node scripts/prepare-capacitor-web.js",
    "sync:android": "npm run prepare:android && cap sync android",
    "android:apk": "npm run sync:android && cd android && JAVA_HOME=/usr/lib/jvm/java-21-temurin-jdk ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
